// find the first occurence of subString s in string t
// easiest solution is to use string::find()
// other way is to implement string::find()
int findSubstring(string &t, string &s){

  for(int i =0; i< s.size(); i++){
    strcmp(t,)
  }
}

// Look at pocket gems solution
